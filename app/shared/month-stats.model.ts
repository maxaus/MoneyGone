export class MonthStats {

    sum: number;
    month: number;
    year: number;

    constructor(sum: number,month: number, year: number) {

        this.sum = sum;
        this.month = month;
        this.year = year;
    }
}